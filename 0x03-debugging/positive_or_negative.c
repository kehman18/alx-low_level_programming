#include "main.h"

/**
* positive_or_negative - this function checks the sign of  a number
*
* @i: this is the number to be checked
*
* Return: always 0
*/

void positive_or_negative(int i)
{
	if (i > 0)
		printf("%d is positive\n", i);
	else
		printf("%d is negative\n", i);
}
